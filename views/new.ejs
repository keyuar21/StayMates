<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <title>Profile</title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        /* Dark Theme and General Styling */
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #121212;
            color: #d1d1d1;
            margin: 0;
            padding-top: 70px; /* Add padding to prevent content from hiding behind navbar */
            min-height: 100vh;
            overflow: hidden;
            transition: background-color 0.5s ease;
            display: flex; /* Use flexbox for body */
            justify-content: center; /* Center content horizontally */
            align-items: flex-start; /* Align content at the top */
        }

        .navbar {
            background: linear-gradient(90deg, #343a40 0%, #1f2d3d 100%);
            padding: 1rem;
            border-bottom: 2px solid #a727278c;
            position: fixed; /* Fix the navbar at the top */
            top: 0; /* Ensure it stays at the top */
            width: 100%; /* Ensure it stretches across the full width */
            z-index: 1000; /* Ensure it stays above other content */
        }
        .navbar-brand {
    color: #ffffff;
    font-weight: bold;
    font-size: 1.25rem;
}

.navbar-nav .nav-link {
    color: #ffffff;
    font-weight: 500;
}

.navbar-nav .nav-link:hover {
    color: #d1d5db; /* Light grey on hover */
}

        
        .profile-container {
            background-color: #1e1e1e;
            width: 90%;
            max-width: 900px; /* Updated max-width for better space usage */
            border-radius: 12px;
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.5);
            padding: 20px;
            text-align: center;
            margin-top: 20px; /* Add some margin for spacing */
            transform: scale(0.95);
            transition: transform 0.5s ease, box-shadow 0.5s ease;
            animation: fadeIn 1s ease-out forwards;
        }

        .profile-container:hover {
            transform: scale(1);
            box-shadow: 0 12px 24px rgba(0, 0, 0, 0.7);
        }

        /* Banner Section */
        .banner {
            background: linear-gradient(135deg, #ff6b6b, #f0f0f0);
            height: 120px;
            display: flex;
            align-items: center;
            justify-content: flex-end;
            padding: 0 20px;
            border-radius: 12px 12px 0 0;
        }

        /* Profile Header */
        .profile-header {
            display: flex;
            flex-direction: column;
            align-items: center;
            margin-top: -60px;
            transition: transform 0.3s ease;
        }

        .profile-picture {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            object-fit: cover;
            border: 4px solid #1e1e1e;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.5);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .profile-picture:hover {
            transform: scale(1.1);
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.7);
        }

        .edit-button {
            padding: 8px 20px;
            font-size: 1em;
            background-color: #2196f3;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.3s ease;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
        }

        .edit-button:hover {
            background-color: #0b7dda;
            transform: translateY(-2px);
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.4);
        }

        .profile-header h1 {
            margin: 10px 0;
            font-size: 2.4em; /* Increased font size for prominence */
            color: #ffffff;
            transition: color 0.3s ease;
        }

        .profile-header p {
            color: #757575;
            font-size: 1.2em;
            transition: color 0.3s ease;
        }

        .profile-header:hover h1,
        .profile-header:hover p {
            color: #ff6b6b; /* Change color on hover */
        }

        .profile-info, .profile-social {
            text-align: left;
            margin: 20px 0;
            animation: slideIn 1s forwards;
            opacity: 0; /* Start with hidden */
            transform: translateY(20px); /* Start with a downward offset */
            transition: opacity 0.5s ease, transform 0.5s ease;
        }

        /* Apply animation to profile-info and profile-social */
        .profile-info.visible,
        .profile-social.visible {
            opacity: 1; /* Fade in */
            transform: translateY(0); /* Move into place */
        }

        /* Profile Information Section */
        .profile-info, .profile-social {
            background-color: #282828;
            padding: 20px;
            border-radius: 8px;
            margin: 20px 0;
        }

        .profile-info h2, .profile-social h2 {
            font-size: 1.6em;
            color: #bbb;
            margin-bottom: 10px;
            border-bottom: 1px solid #444;
            padding-bottom: 5px;
        }

        .profile-info p, .profile-social p {
            font-size: 1em;
            color: #bbb;
            margin: 5px 0;
        }

        /* Social Links */
        .profile-social p a {
            color: #1da1f2;
            text-decoration: none;
            transition: color 0.3s ease;
        }

        .profile-social p a:hover {
            color: #66b2ff;
        }

        /* Keyframes */
        @keyframes fadeIn {
            from { opacity: 0; transform: scale(0.9); }
            to { opacity: 1; transform: scale(1); }
        }

        @keyframes slideIn {
            from { transform: translateY(20px); opacity: 0; }
            to { transform: translateY(0); opacity: 1; }
        }
    </style>
</head>
<body>

    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg bg-body-tertiary mb-3">
        <div class="container-fluid">
            <a class="navbar-brand" href="./index.html">StayMates</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    
                    <li class="nav-item">
                        <form action="/explore" method="GET" style="display: inline;">
                            <button type="submit" class="nav-link btn btn-link">Explore</button>
                        </form>
                    </li>
                    
                    <li class="nav-item">
                        <a class="nav-link" href="./host.html">Hosting</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Use Case 3</a>
                    </li>
                </ul>
                <ul class="navbar-nav mb-2 mb-lg-0">
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false" aria-label="User menu">
                            <% if (profile && profile.profile_picture) { %>
                                <img src="data:image/jpeg;base64,<%= profile.profile_picture.toString('base64') %>" alt="Profile Picture" class="profile-picture" style="width: 40px; height: 40px;">
                            <% } else { %>
                                <img src="/default-profile.jpg" alt="Default Profile Picture" class="profile-picture" style="width: 40px; height: 40px;">
                            <% } %>
                            <strong>Me</strong>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li><a class="dropdown-item" href="./inbox.html">Inbox</a></li>
                            <li><a class="dropdown-item" href="./friendlist.html">Friend list</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="/logout">Sign out</a></li> <!-- Link for sign out -->
                        </ul>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    

    <div class="profile-container">
        <!-- Banner Section -->
        <div class="banner">
            <form action="/profile/edit" method="get">
                <button type="submit" class="edit-button">Edit Profile</button>
            </form>
        </div>
        

        <!-- Profile Header with Profile Picture -->
        <div class="profile-header">
            <% if (profile.profile_picture) { %>
                <img src="data:image/jpeg;base64,<%= profile.profile_picture.toString('base64') %>" alt="Profile Picture" class="profile-picture">
            <% } else { %>
                <img src="/default-profile.jpg" alt="Default Profile Picture" class="profile-picture">
            <% } %>
            <h1><%= profile.full_name %></h1>
            <p>@<%= profile.full_name %></p>
        </div>

        <!-- About Section -->
        <div class="profile-info" id="about-section">
            <h2>About</h2>
            <p><strong>Age:</strong> <%= profile.age %></p>
            <p><strong>Phone:</strong> <%= profile.phone %></p>
            <p><strong>University:</strong> <%= profile.university %></p>
            <p><strong>Address:</strong> <%= profile.address %></p>
            <p><strong>Smoker:</strong> <%= profile.smoker %></p>
            <p><strong>Food Preference:</strong> <%= profile.food %></p>
            <p><strong>Aadhar Number:</strong> <%= profile.adhar_number %></p>
        </div>

        <!-- Social Links Section -->
        <div class="profile-social" id="social-section">
            <h2>Social Links</h2>
            <% if (profile.twitter) { %>
                <p><a href="<%= profile.twitter %>" target="_blank">Twitter</a></p>
            <% } %>
            <% if (profile.instagram) { %>
                <p><a href="<%= profile.instagram %>" target="_blank">Instagram</a></p>
            <% } %>
            <% if (profile.facebook) { %>
                <p><a href="<%= profile.facebook %>" target="_blank">Facebook</a></p>
            <% } %>
        </div>
    </div>

    <script>
        // JavaScript to add visibility class for animations
        window.onload = function() {
            document.getElementById('about-section').classList.add('visible');
            document.getElementById('social-section').classList.add('visible');
        };
    </script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>

</body>
</html>
