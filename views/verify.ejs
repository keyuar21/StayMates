<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>OTP Verification</title>

    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">

    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">

    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f0f2f5;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            overflow: hidden;
            animation: fadeIn 1.2s ease-in-out;
        }

        /* Smooth background color animation */
        @keyframes fadeIn {
            0% { background-color: #ffffff; }
            100% { background-color: #f0f2f5; }
        }

        .form-container {
            background: #ffffff;
            padding: 3rem;
            border-radius: 12px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
            max-width: 400px;
            width: 100%;
            animation: slideIn 0.8s ease-out;
        }

        /* Smooth slide-in effect */
        @keyframes slideIn {
            0% {
                transform: translateY(100px);
                opacity: 0;
            }
            100% {
                transform: translateY(0);
                opacity: 1;
            }
        }

        .form-header {
            margin-bottom: 2rem;
            text-align: center;
            font-size: 1.5rem;
            font-weight: 600;
            color: #333;
            animation: fadeHeader 1s ease-in-out;
        }

        /* Header fade-in animation */
        @keyframes fadeHeader {
            0% { opacity: 0; transform: scale(0.8); }
            100% { opacity: 1; transform: scale(1); }
        }

        .form-control {
            height: calc(2.5em + .75rem + 2px);
            font-size: 1rem;
            padding: 0.75rem;
            border: 2px solid #ced4da;
            border-radius: 8px;
            transition: all 0.3s ease;
        }

        .form-control:focus {
            border-color: #66b2ff;
            box-shadow: 0 0 10px rgba(102, 178, 255, 0.3);
        }

        .btn-success {
            width: 100%;
            background-color: #28a745;
            border-color: #28a745;
            padding: 0.75rem;
            border-radius: 8px;
            font-size: 1.1rem;
            font-weight: 600;
            transition: background-color 0.3s ease, box-shadow 0.3s ease;
        }

        .btn-success:hover {
            background-color: #218838;
            box-shadow: 0 6px 12px rgba(40, 167, 69, 0.3);
        }

        /* Subtle hover effect for the button */
        .btn-success:active {
            background-color: #1e7e34;
            box-shadow: none;
        }

        .form-group {
            animation: fadeInUp 0.9s ease-in-out;
        }

        /* Input fade-up animation */
        @keyframes fadeInUp {
            0% { opacity: 0; transform: translateY(20px); }
            100% { opacity: 1; transform: translateY(0); }
        }

        .form-container::before {
            content: '';
            position: absolute;
            top: -50px;
            left: -50px;
            width: 150px;
            height: 150px;
            background: linear-gradient(135deg, #6b73ff, #000dff);
            border-radius: 50%;
            z-index: -1;
            animation: rotateCircle 6s infinite linear;
        }

        /* Animated background circle */
        @keyframes rotateCircle {
            0% { transform: rotate(0); }
            100% { transform: rotate(360deg); }
        }
    </style>
</head>
<body>

    <div class="form-container">
        <h2 class="form-header">OTP Verification</h2>
        <form action="/verify" method="post">
            <div class="form-group">
                <label for="floatingOtp" class="sr-only">Enter OTP</label>
                <input type="text" class="form-control" id="floatingOtp" name="verifyotp" placeholder="Enter your OTP" required>
            </div>
            <button type="submit" id="verify-otp-btn" class="btn btn-success">Verify OTP</button>
        </form>
    </div>

    <!-- Bootstrap JS and dependencies -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

</body>
</html>
