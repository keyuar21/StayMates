<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Hosted Properties - StayMates</title>

    <!-- Bootstrap 4/5 CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Custom dark theme CSS -->
    <style>
        :root {
            --main-dark: #181512;
            --main-bg: #2d2420;
            --accent: #583a28;
            --accent2: #a67c52;
            --input-bg: #2a2320;
            --input-border: #3a2c22;
            --input-focus: #a67c52;
            --text: #f8f6f2;
            --muted: #d2c6b2;
            --info-bg: #241c18;
            --shadow: 0 4px 24px #583a2822;
        }
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: var(--input-bg);
            color: var(--text);
            min-height: 100vh;
            padding-top: 70px;
        }
        .navbar {
            background: linear-gradient(90deg, var(--main-dark) 0%, var(--accent) 100%);
            padding: 1rem;
            border-bottom: 2px solid var(--accent2);
            position: fixed;
            top: 0;
            width: 100%;
            z-index: 1000;
            box-shadow: 0 2px 12px #18151260;
        }
        .navbar-brand {
            color: var(--accent2);
            font-weight: bold;
            font-size: 1.25rem;
            letter-spacing: 1px;
        }
        .navbar-nav .nav-link {
            color: var(--text);
            font-weight: 500;
        }
        .navbar-nav .nav-link:hover {
            color: var(--accent2);
        }
        .navbar-profile-pic {
            border-radius: 50%;
            object-fit: cover;
            width: 40px;
            height: 40px;
        }
        .main-container {
            max-width: 1100px;
            margin: 2.5rem auto 0 auto;
            padding: 0 1rem;
        }
        .page-header {
            text-align: center;
            margin-bottom: 2.5rem;
        }
        .page-title {
            font-size: 2.5rem;
            font-weight: 700;
            color: var(--accent2);
            margin-bottom: 0.5rem;
            text-shadow: 0 2px 8px #18151260;
        }
        .my-properties-btn {
            background: linear-gradient(135deg, var(--accent2), var(--accent));
            color: var(--main-dark);
            padding: 1rem 2rem;
            border-radius: 12px;
            text-decoration: none;
            font-weight: 600;
            display: inline-block;
            margin: 1rem 0 2rem 0;
            transition: all 0.3s ease;
            border: none;
        }
        .my-properties-btn:hover {
            transform: translateY(-2px);
            box-shadow: var(--shadow);
            color: var(--main-dark);
        }
        .properties-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
            gap: 2.2rem;
        }
        .property-card {
            background: var(--main-dark);
            border: 1.5px solid var(--input-border);
            border-radius: 22px;
            color: var(--text);
            box-shadow: 0 8px 32px rgba(88, 58, 40, 0.18);
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            position: relative;
            overflow: hidden;
            margin-bottom: 2.5rem;
            max-width: 420px;
            margin-left: auto;
            margin-right: auto;
            padding-bottom: 1.5rem;
        }
        .property-card:hover {
            transform: translateY(-8px) scale(1.01);
            box-shadow: 0 16px 48px rgba(88, 58, 40, 0.28);
            border-color: var(--accent2);
        }
        .property-images {
            display: flex;
            gap: 0.7rem;
            overflow-x: auto;
            padding: 1.2rem 1.2rem 0.5rem 1.2rem;
            background: var(--main-bg);
        }
        .property-image {
            width: 180px;
            height: 120px;
            object-fit: cover;
            border-radius: 14px;
            border: 2px solid var(--input-border);
            box-shadow: 0 2px 8px #583a2822;
            background: #fff;
            flex-shrink: 0;
        }
        .property-card h3 {
            color: var(--accent2);
            font-size: 1.3rem;
            font-weight: 700;
            margin: 1.2rem 0 0.5rem 0;
            letter-spacing: 0.5px;
            text-align: center;
        }
        .property-info {
            margin: 0.5rem 1.5rem 0 1.5rem;
        }
        .property-info p {
            margin: 0.4rem 0;
            font-size: 1.05rem;
            color: var(--muted);
        }
        .property-info strong {
            color: var(--accent2);
            font-weight: 600;
        }
        .see-interested-btn {
            background: linear-gradient(135deg, var(--accent2), var(--accent));
            color: #fff;
            border: none;
            font-weight: 600;
            font-size: 1.02rem;
            border-radius: 10px;
            padding: 0.6rem 1.5rem;
            margin-top: 0.7rem;
            box-shadow: 0 2px 8px #583a2822;
            transition: background 0.2s, box-shadow 0.2s, transform 0.2s;
        }
        .see-interested-btn:hover {
            background: linear-gradient(135deg, var(--accent), var(--main-dark));
            color: #fff;
            box-shadow: 0 4px 16px #583a2822;
            transform: translateY(-2px) scale(1.03);
        }
        .footer {
            background: var(--input-bg);
            border-top: 1px solid var(--input-border);
            padding: 12px 0 8px 0;
            margin-top: 28px;
            text-align: center;
        }
        @media (max-width: 600px) {
            .main-container { padding: 0 0.2rem; }
            .properties-grid { grid-template-columns: 1fr; gap: 1.2rem; }
            .property-card { max-width: 98vw; }
            .property-images { padding: 0.7rem 0.2rem 0.3rem 0.2rem; }
        }
    </style>
</head>

<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg bg-body-tertiary mb-3">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">StayMates</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <form action="/explore" method="GET" style="display: inline;">
                            <button type="submit" class="nav-link btn btn-link">Explore</button>
                        </form>
                    </li>
                    <li class="nav-item">
                        <form action="/host_property" method="GET" style="display: inline;">
                            <button type="submit" class="nav-link btn btn-link">Host Property</button>
                        </form>
                    </li>
                    <li class="nav-item">
                        <form action="/view_property" method="GET" style="display: inline;">
                            <button type="submit" class="nav-link btn btn-link">View Property</button>
                        </form>
                    </li>
                </ul>
                <ul class="navbar-nav mb-2 mb-lg-0">
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false" aria-label="User menu">
                            <% if (my_profile && my_profile.profile_picture) { %>
                                <img src="data:image/jpeg;base64,<%= my_profile.profile_picture %>" alt="Profile Picture" class="navbar-profile-pic">
                            <% } else { %>
                                <img src="/default-profile.jpg" alt="Default Profile Picture" class="navbar-profile-pic">
                            <% } %>
                            <strong>Me</strong>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li><a class="dropdown-item" href="/new">My Profile</a></li>
                            <li>
                                <form action="/friendlist" method="GET" class="d-inline">
                                    <button class="dropdown-item" type="submit">Friend List</button>
                                </form>
                            </li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <form action="/logout" method="POST" style="display: inline;">
                                    <button type="submit" class="dropdown-item">Sign out</button>
                                </form>
                            </li>
                        </ul>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    <div class="main-container">
        <div class="page-header">
            <h1 class="page-title">My Hosted Properties</h1>
            <form method="GET" action="/host_property" style="display: inline;">
                <button type="submit" class="my-properties-btn">Host New Property ↗️</button>
            </form>
        </div>
        <% if (properties && properties.length > 0) { %>
            <div class="properties-grid">
                <% properties.forEach(property => { %>
                    <div class="property-card">
                        <div class="property-images">
                            <% property.images.forEach((image, index) => { %>
                                <img src="data:image/jpeg;base64,<%= image %>" alt="Property Image <%= index + 1 %>" class="property-image">
                            <% }) %>
                        </div>
                        <h3><%= property.property_type %> in <%= property.address %></h3>
                        <div class="property-info">
                            <p><strong>Requirement:</strong> <%= property.requirement %></p>
                            <p><strong>Description:</strong> <%= property.description %></p>
                            <p><strong>Rent per Day:</strong> ₹<%= property.rent_per_day %></p>
                            <p><strong>Rent per Week:</strong> ₹<%= property.rent_per_week %></p>
                            <p><strong>Rent per Month:</strong> ₹<%= property.rent_per_month %></p>
                            <p><strong>Occupancy:</strong> <%= property.occupancy %> persons</p>
                            <p><strong>Availability Date:</strong> <%= new Date(property.availability_date).toDateString() %></p>
                            <p><strong>Pets Allowed:</strong> <%= property.pets_allowed === 'Yes' ? "Yes" : "No" %></p>
                            <p><strong>Smoking Allowed:</strong> <%= property.smoking_allowed === 'Yes' ? "Yes" : "No" %></p>
                            <a href="/people_interested/<%= property.property_id %>" class="see-interested-btn">See Interested Users</a>
                        </div>
                    </div>
                <% }) %>
            </div>
        <% } else { %>
            <p class="no-properties">No properties available.</p>
        <% } %>
    </div>
    <footer class="footer">
        <img src="/images/LOGO.png" alt="Logo" style="width:28px; vertical-align:middle; border-radius:5px; margin-right:8px;">
        <span style="color: var(--muted); font-size: 0.98em; vertical-align:middle;">©️2024 Stay-Mates, Inc</span>
    </footer>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
