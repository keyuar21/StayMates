<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Available Properties - StayMates</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        :root {
            --main-dark: #181512;
            --main-bg: #2d2420;
            --accent: #583a28;
            --accent2: #a67c52;
            --input-bg: #2a2320;
            --input-border: #3a2c22;
            --input-focus: #a67c52;
            --text: #f8f6f2;
            --muted: #d2c6b2;
            --info-bg: #241c18;
            --shadow: 0 4px 24px #583a2822;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: var(--main-bg);
            color: var(--text);
            min-height: 100vh;
            line-height: 1.6;
            padding-top: 70px;
        }

        /* Navbar Styles */
        .navbar {
            background: linear-gradient(90deg, var(--main-dark) 0%, var(--accent) 100%);
            padding: 1rem;
            border-bottom: 2px solid var(--accent2);
            position: fixed;
            top: 0;
            width: 100%;
            z-index: 1000;
            box-shadow: 0 2px 12px #18151260;
        }

        .navbar-brand {
            color: var(--accent2);
            font-weight: bold;
            font-size: 1.25rem;
            letter-spacing: 1px;
        }

        .navbar-nav .nav-link {
            color: var(--text);
            font-weight: 500;
        }

        .navbar-nav .nav-link:hover {
            color: var(--accent2);
        }

        .navbar-profile-pic {
            border-radius: 50%;
            object-fit: cover;
            width: 40px;
            height: 40px;
        }

        /* Main Content */
        .main-container {
            max-width: 1200px;
            margin: 2rem auto;
            padding: 0 1rem;
        }

        .page-header {
            text-align: center;
            margin-bottom: 3rem;
        }

        .page-title {
            font-size: 2.5rem;
            font-weight: 700;
            color: var(--accent2);
            margin-bottom: 0.5rem;
            text-shadow: 0 2px 8px #18151260;
        }

        .page-subtitle {
            color: var(--muted);
            font-size: 1.1rem;
        }

        /* Property Cards */
        .properties-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
            gap: 2rem;
            margin-top: 2rem;
        }

        .property-card {
            background: #231a15;
            border: none;
            border-radius: 24px;
            box-shadow: 0 8px 40px 0 rgba(40, 24, 8, 0.28);
            overflow: hidden;
            margin-bottom: 2.5rem;
            max-width: 480px;
            margin-left: auto;
            margin-right: auto;
            padding: 0 0 1.5rem 0;
            position: relative;
            transition: box-shadow 0.3s, transform 0.3s;
        }
        .property-card:hover {
            box-shadow: 0 16px 64px 0 rgba(40, 24, 8, 0.38);
            transform: translateY(-8px) scale(1.01);
        }
        .property-hero {
            position: relative;
            width: 100%;
            height: 320px;
            overflow: hidden;
        }
        .property-hero img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            display: block;
            transition: transform 0.4s cubic-bezier(0.4,0,0.2,1);
        }
        .property-card:hover .property-hero img {
            transform: scale(1.06);
        }
        .property-overlay {
            position: absolute;
            left: 0; right: 0; bottom: 0;
            background: linear-gradient(0deg, rgba(35,26,21,0.92) 60%, rgba(35,26,21,0.0) 100%);
            color: #fff;
            padding: 1.2rem 1.5rem 1rem 1.5rem;
            width: 100%;
            z-index: 2;
        }
        .property-type {
            background: var(--accent2);
            color: #fff;
            font-size: 1.05rem;
            font-weight: 600;
            border-radius: 8px;
            padding: 0.2rem 0.8rem;
            display: inline-block;
            margin-bottom: 0.5rem;
        }
        .property-address {
            font-size: 1.1rem;
            font-weight: 500;
            margin-bottom: 0.2rem;
            color: #fff;
            text-shadow: 0 2px 8px #0008;
        }
        .property-price {
            font-size: 1.25rem;
            font-weight: 700;
            color: var(--accent2);
            margin-bottom: 0.2rem;
        }
        .favorite-icon {
            position: absolute;
            top: 18px;
            right: 22px;
            z-index: 3;
            background: rgba(255,255,255,0.85);
            border-radius: 50%;
            width: 38px;
            height: 38px;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 2px 8px #0002;
            cursor: pointer;
            transition: background 0.2s;
        }
        .favorite-icon:hover {
            background: #fff;
        }
        .favorite-icon svg {
            width: 22px;
            height: 22px;
            fill: #e74c3c;
        }
        .property-info {
            margin: 1.2rem 1.5rem 0 1.5rem;
        }
        .property-info-row {
            display: flex;
            flex-wrap: wrap;
            gap: 1.2rem;
            margin-bottom: 0.7rem;
        }
        .property-feature {
            display: flex;
            align-items: center;
            gap: 0.4rem;
            font-size: 1.02rem;
            color: var(--muted);
        }
        .property-feature svg {
            width: 18px;
            height: 18px;
            fill: var(--accent2);
        }
        .property-description {
            color: #e6d8c8;
            font-size: 1.05rem;
            margin-bottom: 0.7rem;
        }
        @media (max-width: 600px) {
            .property-card {
                max-width: 98vw;
            }
            .property-hero {
                height: 180px;
            }
            .property-info {
                margin: 1rem 0.5rem 0 0.5rem;
            }
        }

        /* Buttons */
        .btn {
            padding: 0.75rem 1.5rem;
            border-radius: 12px;
            font-weight: 600;
            text-decoration: none;
            border: none;
            cursor: pointer;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            display: inline-block;
            text-align: center;
            font-size: 0.95rem;
        }

        .btn-show-interest {
            background: linear-gradient(135deg, var(--accent2), var(--accent));
            color: var(--main-dark);
            position: relative;
            overflow: hidden;
        }

        .btn-show-interest::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.5s ease;
        }

        .btn-show-interest:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(166, 124, 82, 0.3);
        }

        .btn-show-interest:hover::before {
            left: 100%;
        }

        .btn-done {
            background: linear-gradient(135deg, #28a745, #20c997);
            color: white;
            cursor: not-allowed;
        }

        .btn-done:hover {
            transform: none;
            box-shadow: none;
        }

        .btn-accent {
            background: linear-gradient(135deg, var(--accent2), var(--accent));
            color: #fff;
            border: none;
            font-weight: 600;
            font-size: 1.08rem;
            border-radius: 12px;
            padding: 0.75rem 2.2rem;
            box-shadow: 0 2px 8px #583a2822;
            transition: background 0.2s, box-shadow 0.2s, transform 0.2s;
        }
        .btn-accent:hover, .btn-accent:focus {
            background: linear-gradient(135deg, var(--accent), var(--main-dark));
            color: #fff;
            box-shadow: 0 4px 16px #583a2822;
            transform: translateY(-2px) scale(1.03);
        }

        /* No Properties Message */
        .no-properties {
            text-align: center;
            font-size: 1.3rem;
            color: var(--muted);
            margin-top: 3rem;
            padding: 2rem;
            background: var(--info-bg);
            border-radius: 12px;
            border: 1px solid var(--input-border);
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .main-container {
                margin: 1rem auto;
                padding: 0 0.5rem;
            }

            .properties-grid {
                grid-template-columns: 1fr;
                gap: 1.5rem;
            }

            .property-card {
                padding: 1rem;
            }

            .page-title {
                font-size: 2rem;
            }

            .property-images {
                gap: 0.5rem;
            }

            .property-image {
                width: 70px;
                height: 70px;
            }
        }

        /* Animations */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .property-card {
            animation: fadeInUp 0.6s ease-out forwards;
        }

        .property-card:nth-child(1) { animation-delay: 0.1s; }
        .property-card:nth-child(2) { animation-delay: 0.2s; }
        .property-card:nth-child(3) { animation-delay: 0.3s; }
        .property-card:nth-child(4) { animation-delay: 0.4s; }
        .property-card:nth-child(5) { animation-delay: 0.5s; }
        .property-card:nth-child(6) { animation-delay: 0.6s; }

        /* Footer */
        .footer {
            background: var(--input-bg);
            border-top: 1px solid var(--input-border);
            padding: 12px 0 8px 0;
            margin-top: 28px;
            text-align: center;
        }
    </style>
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg bg-body-tertiary mb-3">
        <div class="container-fluid">
            <a class="navbar-brand" href="./index.html">StayMates</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <form action="/explore" method="GET" style="display: inline;">
                            <button type="submit" class="nav-link btn btn-link">Explore</button>
                        </form>
                    </li>
                    <li class="nav-item">
                        <form action="/host_property" method="GET" style="display: inline;">
                            <button type="submit" class="nav-link btn btn-link">Host Property</button>
                        </form>
                    </li>
                    <li class="nav-item">
                        <form action="/view_property" method="GET" style="display: inline;">
                            <button type="submit" class="nav-link btn btn-link">View Property</button>
                        </form>
                    </li>
                </ul>
                <ul class="navbar-nav mb-2 mb-lg-0">
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false" aria-label="User menu">
                            <% if (my_profile && my_profile.profile_picture) { %>
                                <img src="data:image/jpeg;base64,<%= my_profile.profile_picture %>" alt="Profile Picture" class="navbar-profile-pic">
                            <% } else { %>
                                <img src="/default-profile.jpg" alt="Default Profile Picture" class="navbar-profile-pic">
                            <% } %>
                            <strong><%= my_profile && my_profile.full_name ? my_profile.full_name : 'Me' %></strong>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li><a class="dropdown-item" href="/new">My Profile</a></li>
                            <li>
                                <form action="/friendlist" method="GET" class="d-inline">
                                    <button class="dropdown-item" type="submit">Friend List</button>
                                </form>
                            </li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <form action="/logout" method="POST" style="display: inline;">
                                    <button type="submit" class="dropdown-item">Sign out</button>
                                </form>
                            </li>
                        </ul>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="main-container">
        <!-- Page Header -->
        <div class="page-header">
            <h1 class="page-title">Available Properties</h1>
            <p class="page-subtitle">Find your perfect living space</p>
        </div>

        <!-- Properties Grid -->
        <div class="properties-grid">
            <% if (properties && properties.length > 0) { %>
                <% properties.forEach(property => { %>
                    <div class="property-card">
                        <div class="property-hero">
                            <% if (property.images && property.images.length > 0) { %>
                                <% if (property.images.length === 1) { %>
                                    <img src="data:image/jpeg;base64,<%= property.images[0] %>" alt="Property Main Image" style="width:100%;height:100%;object-fit:cover;display:block;border-radius:0;">
                                <% } else { %>
                                    <div id="carousel-<%= property.property_id %>" class="carousel slide" data-bs-ride="carousel" style="width:100%;height:100%;">
                                        <div class="carousel-inner" style="border-radius:0;">
                                            <% property.images.forEach((image, idx) => { %>
                                                <div class="carousel-item <%= idx === 0 ? 'active' : '' %>">
                                                    <img src="data:image/jpeg;base64,<%= image %>" class="d-block w-100" alt="Property Image <%= idx + 1 %>" style="height:320px;object-fit:cover;">
                                                </div>
                                            <% }) %>
                                        </div>
                                        <button class="carousel-control-prev" type="button" data-bs-target="#carousel-<%= property.property_id %>" data-bs-slide="prev">
                                            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                                            <span class="visually-hidden">Previous</span>
                                        </button>
                                        <button class="carousel-control-next" type="button" data-bs-target="#carousel-<%= property.property_id %>" data-bs-slide="next">
                                            <span class="carousel-control-next-icon" aria-hidden="true"></span>
                                            <span class="visually-hidden">Next</span>
                                        </button>
                                        <div class="carousel-indicators" style="bottom:10px;">
                                            <% property.images.forEach((image, idx) => { %>
                                                <button type="button" data-bs-target="#carousel-<%= property.property_id %>" data-bs-slide-to="<%= idx %>" class="<%= idx === 0 ? 'active' : '' %>" aria-current="<%= idx === 0 ? 'true' : 'false' %>" aria-label="Slide <%= idx + 1 %>"></button>
                                            <% }) %>
                                        </div>
                                    </div>
                                <% } %>
                            <% } else { %>
                                <img src="/default-property.jpg" alt="No Image Available" style="width:100%;height:100%;object-fit:cover;display:block;border-radius:0;">
                            <% } %>
                            <div class="favorite-icon" title="Add to favorites">
                                <svg viewBox="0 0 24 24"><path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41 0.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/></svg>
                            </div>
                            <div class="property-overlay">
                                <div class="property-type"><%= property.property_type %></div>
                                <div class="property-address"><%= property.address %></div>
                                <div class="property-price">₹<%= property.rent_per_month %> / month</div>
                            </div>
                        </div>
                        <div class="property-info">
                            <div class="property-info-row">
                                <div class="property-feature" title="Requirement">
                                    <svg viewBox="0 0 24 24"><path d="M19 3H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2zm0 16H5V5h14v14z"/></svg>
                                    <span><strong>Requirement:</strong> <%= property.requirement %></span>
                                </div>
                                <div class="property-feature" title="Occupancy">
                                    <svg viewBox="0 0 24 24"><path d="M12 12c2.7 0 8 1.34 8 4v2H4v-2c0-2.66 5.3-4 8-4zm0-2a4 4 0 1 0 0-8 4 4 0 0 0 0 8z"/></svg>
                                    <span><strong>Occupancy:</strong> <%= property.occupancy %> persons</span>
                                </div>
                                <div class="property-feature" title="Availability Date">
                                    <svg viewBox="0 0 24 24"><path d="M19 4h-1V2h-2v2H8V2H6v2H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2zm0 16H5V8h14v12z"/></svg>
                                    <span><strong>Available:</strong> <%= new Date(property.availability_date).toDateString() %></span>
                                </div>
                            </div>
                            <div class="property-info-row">
                                <div class="property-feature" title="Rent per Day">
                                    <svg viewBox="0 0 24 24"><path d="M12 17c-1.1 0-2 .9-2 2s.9 2 2 2 2-.9 2-2-.9-2-2-2zm-6.6-1.2c-.2-.2-.4-.5-.4-.8 0-.6.4-1 1-1 .3 0 .6.2.8.4l1.8 1.8c.2.2.2.5 0 .7-.2.2-.5.2-.7 0l-1.8-1.8zm13.2 0l-1.8 1.8c-.2.2-.5.2-.7 0-.2-.2-.2-.5 0-.7l1.8-1.8c.2-.2.5-.2.8-.4.6 0 1 .4 1 1 0 .3-.2.6-.4.8z"/></svg>
                                    <span><strong>Day:</strong> ₹<%= property.rent_per_day %></span>
                                </div>
                                <div class="property-feature" title="Rent per Week">
                                    <svg viewBox="0 0 24 24"><path d="M12 17c-1.1 0-2 .9-2 2s.9 2 2 2 2-.9 2-2-.9-2-2-2zm-6.6-1.2c-.2-.2-.4-.5-.4-.8 0-.6.4-1 1-1 .3 0 .6.2.8.4l1.8 1.8c.2.2.2.5 0 .7-.2.2-.5.2-.7 0l-1.8-1.8zm13.2 0l-1.8 1.8c-.2.2-.5.2-.7 0-.2-.2-.2-.5 0-.7l1.8-1.8c.2-.2.5-.2.8-.4.6 0 1 .4 1 1 0 .3-.2.6-.4.8z"/></svg>
                                    <span><strong>Week:</strong> ₹<%= property.rent_per_week %></span>
                                </div>
                                <div class="property-feature" title="Rent per Month">
                                    <svg viewBox="0 0 24 24"><path d="M12 17c-1.1 0-2 .9-2 2s.9 2 2 2 2-.9 2-2-.9-2-2-2zm-6.6-1.2c-.2-.2-.4-.5-.4-.8 0-.6.4-1 1-1 .3 0 .6.2.8.4l1.8 1.8c.2.2.2.5 0 .7-.2.2-.5.2-.7 0l-1.8-1.8zm13.2 0l-1.8 1.8c-.2.2-.5.2-.7 0-.2-.2-.2-.5 0-.7l1.8-1.8c.2-.2.5-.2.8-.4.6 0 1 .4 1 1 0 .3-.2.6-.4.8z"/></svg>
                                    <span><strong>Month:</strong> ₹<%= property.rent_per_month %></span>
                                </div>
                            </div>
                            <div class="property-info-row">
                                <div class="property-feature" title="Pets Allowed">
                                    <svg viewBox="0 0 24 24"><path d="M12 17c-1.1 0-2 .9-2 2s.9 2 2 2 2-.9 2-2-.9-2-2-2zm-6.6-1.2c-.2-.2-.4-.5-.4-.8 0-.6.4-1 1-1 .3 0 .6.2.8.4l1.8 1.8c.2.2.2.5 0 .7-.2.2-.5.2-.7 0l-1.8-1.8zm13.2 0l-1.8 1.8c-.2.2-.5.2-.7 0-.2-.2-.2-.5 0-.7l1.8-1.8c.2-.2.5-.2.8-.4.6 0 1 .4 1 1 0 .3-.2.6-.4.8z"/></svg>
                                    <span><strong>Pets:</strong> <%= property.pets_allowed === 'Yes' ? 'Allowed' : 'Not Allowed' %></span>
                                </div>
                                <div class="property-feature" title="Smoking Allowed">
                                    <svg viewBox="0 0 24 24"><path d="M20 19v-1c0-1.1-.9-2-2-2H6c-1.1 0-2 .9-2 2v1H2v2h20v-2h-2zm-2-3c1.1 0 2-.9 2-2V7c0-1.1-.9-2-2-2H6C4.9 5 4 5.9 4 7v7c0 1.1.9 2 2 2h12z"/></svg>
                                    <span><strong>Smoking:</strong> <%= property.smoking_allowed === 'Yes' ? 'Allowed' : 'Not Allowed' %></span>
                                </div>
                            </div>
                            <div class="property-description">
                                <strong>Description:</strong> <%= property.description %>
                            </div>
                        </div>
                        <!-- Show Interest Button -->
                        <div style="text-align:center; margin-top: 1.2rem;">
                          <% if (my_id) { %>
                            <form action="/show_interest/<%= property.property_id %>" method="POST" id="interest-form-<%= property.property_id %>" style="display:inline-block;">
                              <input type="hidden" name="user_id" value="<%= my_id %>">
                              <button type="submit" class="btn btn-accent btn-lg" id="interest-btn-<%= property.property_id %>" style="min-width:180px; font-size:1.08rem; font-weight:600; margin-top:0.2rem;">Show Interest</button>
                            </form>
                          <% } else { %>
                            <p style="color: var(--muted); font-style: italic; margin-top:0.5rem;">Please log in to show interest.</p>
                          <% } %>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <div class="no-properties">
                    <p>No properties available at the moment.</p>
                    <p style="font-size: 1rem; margin-top: 1rem;">Check back later for new listings!</p>
                </div>
            <% } %>
        </div>
    </div>

    <!-- Footer -->
    <footer class="footer">
        <img src="/images/LOGO.png" alt="Logo" style="width:28px; vertical-align:middle; border-radius:5px; margin-right:8px;">
        <span style="color: var(--muted); font-size: 0.98em; vertical-align:middle;">©️2024 Stay-Mates, Inc</span>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Handle the "Show Interest" button click event
        function handleShowInterest(event, propertyId) {
            event.preventDefault(); // Prevent form submission

            // Get the button and form elements
            const btn = document.getElementById(`interest-btn-${propertyId}`);
            const form = document.getElementById(`interest-form-${propertyId}`);

            // Change the button to "Done" and make it green
            btn.innerHTML = "Done";
            btn.classList.remove("btn-show-interest");
            btn.classList.add("btn-done");

            // Disable the button to prevent further clicks
            btn.disabled = true;

            // Optionally, submit the form after a short delay (for visual effect)
            setTimeout(() => {
                form.submit();
            }, 1000); // 1 second delay for visual effect
        }
    </script>
</body>
</html>
