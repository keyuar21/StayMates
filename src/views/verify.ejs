<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verify Your OTP | StayMates</title>
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@500;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --main-brown: #583a28;
        }
        body {
            font-family: 'Montserrat', sans-serif;
            background: linear-gradient(120deg, #f7ede2 0%, #f1dac4 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .verify-container {
            background: #fff;
            border-radius: 18px;
            box-shadow: 0 8px 32px rgba(88,58,40,0.10);
            padding: 2.5rem 2rem 2rem 2rem;
            max-width: 400px;
            width: 100%;
            position: relative;
            /* Animation */
            opacity: 0;
            transform: translateY(60px) scale(0.98);
            animation: fadeSlideIn 1.1s cubic-bezier(.77,0,.18,1) 0.1s forwards;
        }
        @keyframes fadeSlideIn {
            0% {
                opacity: 0;
                transform: translateY(60px) scale(0.98);
            }
            60% {
                opacity: 1;
                transform: translateY(-8px) scale(1.03);
            }
            100% {
                opacity: 1;
                transform: translateY(0) scale(1);
            }
        }
        .brand {
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 1.5rem;
        }
        .brand-logo {
            width: 42px;
            height: 42px;
            margin-right: 0.7rem;
            display: flex;
            align-items: center;
            justify-content: center;
            /* Pulse animation */
            animation: pulseLock 1.8s infinite cubic-bezier(.66,0,.34,1);
        }
        @keyframes pulseLock {
            0% { transform: scale(1); filter: drop-shadow(0 0 0 #583a2822); }
            50% { transform: scale(1.10); filter: drop-shadow(0 0 8px #583a2844); }
            100% { transform: scale(1); filter: drop-shadow(0 0 0 #583a2822); }
        }
        .brand-title {
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--main-brown);
            letter-spacing: 1px;
        }
        .verify-title {
            font-size: 1.25rem;
            font-weight: 600;
            color: #3d2617;
            text-align: center;
            margin-bottom: 0.5rem;
        }
        .verify-desc {
            color: #7c5a43;
            font-size: 1rem;
            text-align: center;
            margin-bottom: 1.5rem;
        }
        .form-label {
            font-weight: 500;
            color: #583a28;
        }
        .form-control {
            border-radius: 8px;
            font-size: 1.1rem;
            padding: 0.7rem 1rem;
            border: 2px solid #e7c9b2;
        }
        .form-control:focus {
            border-color: var(--main-brown);
            box-shadow: 0 0 0 0.2rem rgba(88,58,40,0.08);
        }
        .btn-primary {
            background: var(--main-brown);
            border: none;
            border-radius: 8px;
            font-weight: 600;
            font-size: 1.1rem;
            padding: 0.7rem 0;
            transition: background 0.2s;
        }
        .btn-primary:hover {
            background: #3d2617;
        }
        .security-hint {
            font-size: 0.95rem;
            color: #a98b6d;
            margin-top: 1rem;
            text-align: center;
        }
        .resend-link {
            display: block;
            text-align: center;
            margin-top: 1.2rem;
            color: var(--main-brown);
            font-weight: 500;
            text-decoration: none;
            transition: color 0.2s;
        }
        .resend-link:hover {
            color: #3d2617;
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="verify-container">
        <div class="brand">
            <span class="brand-logo">
                <!-- Elegant lock SVG icon in #583a28 -->
                <svg width="36" height="36" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                  <rect x="4" y="10" width="16" height="10" rx="3" fill="#583a28"/>
                  <rect x="9" y="6" width="6" height="6" rx="3" fill="#583a28"/>
                  <rect x="7" y="4" width="10" height="8" rx="5" fill="#fff"/>
                  <rect x="10.5" y="14" width="3" height="5" rx="1.5" fill="#fff"/>
                </svg>
            </span>
            <span class="brand-title">StayMates</span>
        </div>
        <div class="verify-title">Verify Your Email</div>
        <div class="verify-desc">
            Please enter the 6-digit OTP sent to your email address.<br>
            This helps us keep your account secure.
        </div>
        <form action="/verify" method="post" autocomplete="off">
            <div class="mb-3">
                <label for="otpInput" class="form-label">One-Time Password (OTP)</label>
                <input type="text" class="form-control text-center" id="otpInput" name="verifyotp" maxlength="6" minlength="6" pattern="\d{6}" placeholder="Enter 6-digit OTP" required autofocus>
            </div>
            <button type="submit" class="btn btn-primary w-100">Verify OTP</button>
        </form>
        <div class="security-hint">
            <span>Didn't receive the code?</span>
            <a href="/sendotp" class="resend-link">Resend OTP</a>
        </div>
    </div>
    <!-- Bootstrap 5 JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
